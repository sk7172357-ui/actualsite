Ты — senior full-stack developer и DevOps, хорошо знакомый с Mastra, Railway и переносимыми Node.js-проектами.

⸻

⚠️ КРИТИЧЕСКОЕ ПРАВИЛО (НЕ НАРУШАТЬ)

❌ Никогда не менять основной проект, который сейчас работает на Replit.

✅ Все изменения выполнять ТОЛЬКО в КОПИИ проекта, предназначенной для нового GitHub-репозитория.
Основной проект остаётся нетронутым.

⸻

Исходные условия
 • Основной проект (сайт + Telegram-бот) корректно работает на Replit.
 • Его копия, загруженная в GitHub и задеплоенная на Railway, НЕ работает.
 • На Railway появляются ошибки:
 • Internal Server Error
 • Admin login page not found
 • ENOENT: no such file or directory
 • отсутствие TELEGRAM_GROUP_BOT_TOKEN

⸻

Цель

Создать ПОЛНОСТЬЮ ПЕРЕНОСИМУЮ КОПИЮ проекта, которая:
 • работает локально, на Railway и других хостингах;
 • запускается командами: npm install
npm start
• ведёт себя 1:1 как рабочая версия на Replit;
 • не содержит заглушек вида "Welcome to Ticket System / Server is running";
 • не падает из-за отсутствующих ENV или файлов.

⸻

Обязательные исправления (ТОЛЬКО В КОПИИ)

1️⃣ package.json
 • Удалить ВСЕ merge-конфликты (<<<<<<<, =======, >>>>>>>).
 • Сделать файл валидным JSON.
 • Один корректный start-скрипт, используемый Mastra.

⸻

2️⃣ Пути к файлам (КРИТИЧЕСКИ ВАЖНО)

На Railway возникает ошибка: ENOENT: no such file or directory,
/home/runner/workspace/src/mastra/public/admin-events.html
Необходимо:
 • найти реальное расположение HTML-файлов, которые используются в рабочей версии;
 • убрать жёсткие относительные пути, зависящие от Replit;
 • использовать абсолютные и безопасные пути (path.resolve, import.meta.url);
 • гарантировать, что:
 • admin-events.html
 • generator.html
 • и другие public-файлы
доступны на любом хостинге.

❌ Запрещено:
 • удалять страницы;
 • заменять их заглушками;
 • менять бизнес-логику.

⸻

3️⃣ Админка и маршруты
 • Админ-страница ОБЯЗАНА существовать.
 • Ошибка "Admin login page not found" должна быть устранена.
 • Все маршруты должны совпадать с рабочей версией на Replit.

⸻

4️⃣ Telegram-бот

Сейчас в логах: ❌ [TelegramAdmin] TELEGRAM_GROUP_BOT_TOKEN not configured
Нужно:
 • перевести Telegram-бота полностью на ENV-переменные: TELEGRAM_BOT_TOKEN=
ADMIN_TELEGRAM_ID=
 • бот должен уметь писать администратору в личные сообщения;
 • если ENV не заданы:
 • выводить предупреждение в лог;
 • НЕ ронять сервер.

⸻

5️⃣ Переносимость
 • Использовать: process.env.PORT
  • Никакой привязки к:
 • Replit
 • конкретной файловой структуре среды
 • Проект должен одинаково работать:
 • локально
 • на Railway
 • на любом Node-хостинге

⸻

Что категорически запрещено

❌ Менять основной проект
❌ Менять поведение сайта
❌ Делать «упрощённую» версию
❌ Маскировать ошибки заглушками

⸻

Результат
 • Новый GitHub-репозиторий с рабочей копией проекта
 • Отсутствие Internal Server Error
 • Все страницы и админка работают
 • Railway логи чистые
 • Поведение 1:1 как на Replit