У меня есть GitHub-репозиторий, полученный из проекта, который изначально работал на Replit, но ПОСЛЕ переноса на GitHub и деплоя на Railway он работает некорректно.

❗ Replit-версия рабочая и трогать её нельзя.
❗ Работать нужно ТОЛЬКО с кодом в GitHub-репозитории.

⸻

Текущие проблемы
 1. При открытии главной страницы отображается только:
Welcome to Ticket System
Server is running.
2. Все остальные страницы возвращают:
 • {"error":"Internal Server Error"}
 • Admin login page not found
 3. После деплоя на Railway сайт не работает как на Replit
Задачи

1. Восстановить работоспособность сайта
 • Проверить и исправить:
 • точки входа (server.js, index.js, app.py и т.п.);
 • роутинг (Express / FastAPI / Flask);
 • пути к страницам и API;
 • работу admin-панели;
 • загрузку статики (public, static, build);
 • Убедиться, что:
 • сайт открывается полностью;
 • все страницы работают;
 • admin-страница доступна;
 • нет 500 Internal Server Error.

2. Railway-совместимость
 • Порт брать строго из process.env.PORT / PORT
 • Убедиться, что:
 • сервер запускается одной командой;
 • нет Replit-specific логики;
 • сайт работает на Railway без изменения кода

⸻

3. Telegram-бот (ОЧЕНЬ ВАЖНО)

Нужно сделать, чтобы:
 • я мог менять Telegram-бота без изменения кода;
 • бот мог отправлять сообщения мне в личные сообщения;
 • BOT_TOKEN и ADMIN_TELEGRAM_ID задавались через ENV;
 • не было жёсткой привязки к группе или каналу.

Пример требований:
 • если ADMIN_TELEGRAM_ID задан → бот может писать в личку;
 • если нет → выводить понятную ошибку в логах.

⸻

4. Логи и ошибки
 • Добавить нормальные логи:
 • причины Internal Server Error;
 • отсутствия admin-страницы;
 • Ошибки не должны маскироваться.

⸻

5. Что запрещено

❌ Нельзя менять рабочую версию на Replit
❌ Нельзя хардкодить токены, chat_id, домены
❌ Нельзя делать логику, завязанную на конкретный хостинг

⸻

Результат
 • GitHub-репозиторий с исправленным кодом
 • Сайт:
 • полностью работает на Railway;
 • ведёт себя так же, как на Replit;
 • Telegram-бот:
 • легко меняется;
 • может писать в личку администратору;
 • Никаких Internal Server Error
 • Никаких Admin page not found

Если какая-то часть сайта не работает — найди и исправь причину, а не заглушай ошибку.